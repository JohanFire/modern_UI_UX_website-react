import React from 'react'

// you can import components with this line, but in this case I will not do it like this
// I will import the components inside our containers
import { Article, Brand, CTA, Feature, Navbar } from './components'

const App = () => {
    return (
        <div>
            <h1>GPT-3</h1>
        </div>
    )
}

export default App
